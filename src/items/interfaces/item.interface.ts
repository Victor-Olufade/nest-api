export interface Item {
  id?: string;
  name: string;
  description?: string;
  qty: number;
}

export interface updateItem {
  id?: string;
  name?: string;
  description?: string;
  qty?: number;
}
